language: python
python:
  - "2.7"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install build-essential libgeos-c1 libprotobuf-dev protobuf-compiler
install:
  - pip install -q -r requirements.txt --use-mirrors
script:
  - python setup.py test
notifications:
  irc:
    channels:
      - "irc.freenode.org#molly"
    on_success: change